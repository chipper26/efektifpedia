name: Efektifpedia AI Writer Bot

on:
  schedule:
    # Menggunakan menit ke-10 untuk menghindari beban puncak server GitHub pada menit ke-0
    # 00:10 UTC = 07:10 WIB (Berita Pagi)
    # 06:10 UTC = 13:10 WIB (Berita Siang)
    # 12:10 UTC = 19:10 WIB (Berita Malam)
    - cron: '10 0,6,12 * * *'
  workflow_dispatch: 

jobs:
  write:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Requests
        run: pip install requests

      - name: Generate Content
        env:
          # Menghapus spasi ekstra di dalam bracket untuk kompatibilitas maksimal
          OPENROUTER_API_KEY: ${{secrets.OPENROUTER_API_KEY}}
          PEXELS_API_KEY: ${{secrets.PEXELS_API_KEY}}
        run: python ai_writer.py

      - name: Commit and Push
        run: |
          git config --local user.email "bot@efektifpedia.com"
          git config --local user.name "Efektifpedia Bot"
          git add .
          # Menggunakan $(date) agar pesan commit selalu unik dan mudah dilacak
          git commit -m "AI: Auto Update Content - $(date)" || exit 0
          git push